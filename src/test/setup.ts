import '@testing-library/jest-dom'
import { vi } from 'vitest'

// Mock implementation for Next.js features and other browser APIs if needed
// For example, mocking global APIs:
// global.fetch = vi.fn()
